# ISO 27001 Fase 4 — Registro y Monitoreo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar trazabilidad completa de acciones (login, CRUD, acceso a Drive) con tabla `audit_log` en Supabase y UI de revisión para el admin en `/dashboard/admin/ajustes/registros`.

**Architecture:** Helper server-only `lib/audit.ts` que usa `createAdminClient()` (ya existe) para insertar en `audit_log` bypasseando RLS. Cada server action llama `logAuditEvent()` al final de la operación exitosa. Los links a Drive se pasan por `/api/drive-redirect` que loggea antes de redirigir. La UI de logs usa RSC + searchParams para filtros sin estado client.

**Tech Stack:** Next.js App Router, Supabase (PostgreSQL + RLS), `next/headers`, `@supabase/supabase-js` service role client, Tailwind CSS con design tokens del proyecto (`primary`, `surface-container`, `error`, etc.), `react-icons/lu`.

**Spec:** `docs/superpowers/specs/2026-05-07-iso27001-fase-4-design.md`

---

## File Map

| Archivo | Acción |
|---------|--------|
| `lib/audit.ts` | Crear — helper `logAuditEvent` |
| `app/api/drive-redirect/route.ts` | Crear — loggea y redirige a Drive |
| `app/dashboard/admin/ajustes/registros/page.tsx` | Crear — RSC viewer |
| `app/dashboard/admin/ajustes/registros/AuditTable.tsx` | Crear — client filters + tabla |
| `app/actions/auth.ts` | Modificar — login, login_fallido, logout |
| `app/actions/empresas.ts` | Modificar — CRUD logging |
| `app/actions/sucursales.ts` | Modificar — CRUD logging |
| `app/actions/usuarios.ts` | Modificar — CRUD logging |
| `app/actions/chequeos.ts` | Modificar — CRUD logging |
| `app/actions/chequeos-pre-empleo.ts` | Modificar — CRUD logging |
| `app/actions/settings.ts` | Modificar — cambio_nombre, cambio_correo, cambio_contraseña |
| `app/dashboard/responsable/periodico/[año]/PeriodicoChequeos.tsx` | Modificar — href → drive-redirect |
| `app/dashboard/responsable/pre-empleo/[año]/[sucursalId]/[mes]/PreEmpleoDaySelector.tsx` | Modificar — href → drive-redirect |
| `app/dashboard/admin/ajustes/AjustesClient.tsx` | Modificar — agregar link a registros |

---

## Task 1: Aplicar migración SQL — tabla `audit_log`

**Files:**
- Supabase dashboard / MCP tool

- [ ] **Step 1: Aplicar migración via Supabase MCP**

Usar `mcp__plugin_supabase_supabase__apply_migration` con `name: "add_audit_log"` y el siguiente SQL:

```sql
create table audit_log (
  id uuid primary key default gen_random_uuid(),
  usuario_id uuid references profiles(id) on delete set null,
  accion text not null,
  recurso text,
  ip_address text,
  user_agent text,
  created_at timestamptz default now()
);

alter table audit_log enable row level security;

create policy "admin_select_audit" on audit_log
  for select using (
    exists (select 1 from profiles where id = auth.uid() and rol = 'admin')
  );
```

- [ ] **Step 2: Verificar tabla creada**

Usar `mcp__plugin_supabase_supabase__list_tables` y confirmar que `audit_log` aparece en la lista.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat(audit): add audit_log table with admin-only RLS policy"
```

---

## Task 2: Crear helper `lib/audit.ts`

**Files:**
- Create: `lib/audit.ts`

`createAdminClient()` ya existe en `lib/supabase/admin.ts` y usa `SUPABASE_SERVICE_ROLE_KEY`. No se necesita archivo nuevo de supabase client.

- [ ] **Step 1: Crear `lib/audit.ts`**

```typescript
import { headers } from 'next/headers'
import { createClient } from '@/lib/supabase/server'
import { createAdminClient } from '@/lib/supabase/admin'

export type AuditAction =
  | 'login'
  | 'login_fallido'
  | 'logout'
  | 'acceso_drive'
  | 'crear_empresa'
  | 'editar_empresa'
  | 'eliminar_empresa'
  | 'crear_sucursal'
  | 'editar_sucursal'
  | 'eliminar_sucursal'
  | 'crear_usuario'
  | 'editar_usuario'
  | 'eliminar_usuario'
  | 'crear_chequeo'
  | 'editar_chequeo'
  | 'eliminar_chequeo'
  | 'crear_chequeo_pre_empleo'
  | 'editar_chequeo_pre_empleo'
  | 'eliminar_chequeo_pre_empleo'
  | 'cambio_nombre'
  | 'cambio_correo'
  | 'cambio_contraseña'

export async function logAuditEvent(
  accion: AuditAction,
  recurso?: string,
  usuarioId?: string,
): Promise<void> {
  try {
    const h = await headers()
    const ip =
      h.get('x-forwarded-for')?.split(',')[0]?.trim() ??
      h.get('x-real-ip') ??
      null
    const userAgent = h.get('user-agent') ?? null

    let resolvedUserId = usuarioId ?? null
    if (!resolvedUserId) {
      const supabase = await createClient()
      const {
        data: { user },
      } = await supabase.auth.getUser()
      resolvedUserId = user?.id ?? null
    }

    const admin = createAdminClient()
    await admin.from('audit_log').insert({
      usuario_id: resolvedUserId,
      accion,
      recurso: recurso ?? null,
      ip_address: ip,
      user_agent: userAgent,
    })
  } catch {
    // audit failure never blocks main flow
  }
}
```

- [ ] **Step 2: Verificar que TypeScript compila**

```bash
npx tsc --noEmit
```

Expected: sin errores relacionados a `lib/audit.ts`.

- [ ] **Step 3: Commit**

```bash
git add lib/audit.ts
git commit -m "feat(audit): add logAuditEvent server-side helper"
```

---

## Task 3: Wire auth actions — login, login_fallido, logout

**Files:**
- Modify: `app/actions/auth.ts`

- [ ] **Step 1: Reemplazar contenido de `app/actions/auth.ts`**

```typescript
'use server'

import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { getProfile } from '@/lib/auth'
import { logAuditEvent } from '@/lib/audit'

export type LoginState = {
  error?: string
  redirectTo?: string
} | null

export async function login(_state: LoginState, formData: FormData): Promise<LoginState> {
  const email = formData.get('email') as string
  const password = formData.get('password') as string

  if (!email || !password) {
    return { error: 'Completa todos los campos.' }
  }

  const supabase = await createClient()
  const { data, error } = await supabase.auth.signInWithPassword({ email, password })

  if (error) {
    await logAuditEvent('login_fallido', email)
    return { error: 'Credenciales incorrectas. Verifica tu email y contraseña.' }
  }

  await logAuditEvent('login', email, data.user?.id)

  const profile = await getProfile()
  const redirectTo = profile?.rol === 'admin'
    ? '/dashboard/admin/empresas'
    : '/dashboard/responsable'

  return { redirectTo }
}

export async function logout() {
  await logAuditEvent('logout')
  const supabase = await createClient()
  await supabase.auth.signOut()
  redirect('/login?logout=1')
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Expected: sin errores.

- [ ] **Step 3: Verificar manualmente**

```bash
pnpm dev
```

1. Ir a `/login`, ingresar credenciales incorrectas.
2. Ir a Supabase dashboard → Table Editor → `audit_log`. Confirmar fila con `accion = 'login_fallido'` y `recurso = <email>`.
3. Ingresar credenciales correctas. Confirmar fila `accion = 'login'` con `usuario_id` no nulo.
4. Hacer logout. Confirmar fila `accion = 'logout'`.

- [ ] **Step 4: Commit**

```bash
git add app/actions/auth.ts
git commit -m "feat(audit): log login, login_fallido, and logout events"
```

---

## Task 4: Wire CRUD actions — empresas y sucursales

**Files:**
- Modify: `app/actions/empresas.ts`
- Modify: `app/actions/sucursales.ts`

- [ ] **Step 1: Reemplazar `app/actions/empresas.ts`**

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { requireAdmin } from '@/lib/auth'
import { logAuditEvent } from '@/lib/audit'

export async function createEmpresa(formData: FormData) {
  await requireAdmin()
  const nombre = (formData.get('nombre') as string).trim()
  if (!nombre) return { error: 'El nombre es requerido' }

  const supabase = await createClient()
  const { error } = await supabase.from('empresas').insert({ nombre })
  if (error) return { error: error.message }

  await logAuditEvent('crear_empresa', nombre)
  revalidatePath('/dashboard/admin/empresas')
}

export async function updateEmpresa(id: string, formData: FormData) {
  await requireAdmin()
  const nombre = (formData.get('nombre') as string).trim()
  if (!nombre) return { error: 'El nombre es requerido' }

  const supabase = await createClient()
  const { error } = await supabase.from('empresas').update({ nombre }).eq('id', id)
  if (error) return { error: error.message }

  await logAuditEvent('editar_empresa', nombre)
  revalidatePath('/dashboard/admin/empresas')
}

export async function deleteEmpresa(id: string) {
  await requireAdmin()
  const supabase = await createClient()
  const { error } = await supabase.from('empresas').delete().eq('id', id)
  if (error) return { error: error.message }

  await logAuditEvent('eliminar_empresa', id)
  revalidatePath('/dashboard/admin/empresas')
}
```

- [ ] **Step 2: Reemplazar `app/actions/sucursales.ts`**

```typescript
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { requireAdmin } from '@/lib/auth'
import { logAuditEvent } from '@/lib/audit'

export async function createSucursal(formData: FormData) {
  await requireAdmin()
  const nombre = (formData.get('nombre') as string).trim()
  const empresa_id = formData.get('empresa_id') as string
  if (!nombre) return { error: 'El nombre es requerido' }
  if (!empresa_id) return { error: 'Selecciona una empresa' }

  const supabase = await createClient()
  const { error } = await supabase.from('sucursales').insert({ nombre, empresa_id })
  if (error) return { error: error.message }

  await logAuditEvent('crear_sucursal', nombre)
  revalidatePath('/dashboard/admin/sucursales')
}

export async function updateSucursal(id: string, formData: FormData) {
  await requireAdmin()
  const nombre = (formData.get('nombre') as string).trim()
  const empresa_id = formData.get('empresa_id') as string
  if (!nombre) return { error: 'El nombre es requerido' }
  if (!empresa_id) return { error: 'Selecciona una empresa' }

  const supabase = await createClient()
  const { error } = await supabase
    .from('sucursales')
    .update({ nombre, empresa_id })
    .eq('id', id)
  if (error) return { error: error.message }

  await logAuditEvent('editar_sucursal', nombre)
  revalidatePath('/dashboard/admin/sucursales')
}

export async function deleteSucursal(id: string) {
  await requireAdmin()
  const supabase = await createClient()
  const { error } = await supabase.from('sucursales').delete().eq('id', id)
  if (error) return { error: error.message }

  await logAuditEvent('eliminar_sucursal', id)
  revalidatePath('/dashboard/admin/sucursales')
}
```

- [ ] **Step 3: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add app/actions/empresas.ts app/actions/sucursales.ts
git commit -m "feat(audit): log CRUD events for empresas and sucursales"
```

---

## Task 5: Wire CRUD actions — usuarios, chequeos, settings

**Files:**
- Modify: `app/actions/usuarios.ts`
- Modify: `app/actions/chequeos.ts`
- Modify: `app/actions/chequeos-pre-empleo.ts`
- Modify: `app/actions/settings.ts`

- [ ] **Step 1: Agregar imports y llamadas en `app/actions/usuarios.ts`**

Agregar al principio del archivo (después de los imports existentes):

```typescript
import { logAuditEvent } from '@/lib/audit'
```

Luego agregar `logAuditEvent` al final de cada función exitosa:

En `createUsuario`, antes del `revalidatePath`:
```typescript
await logAuditEvent('crear_usuario', email)
```

En `updateUsuario`, antes del `revalidatePath`:
```typescript
await logAuditEvent('editar_usuario', id)
```

En `deleteUsuario`, antes del `revalidatePath`:
```typescript
await logAuditEvent('eliminar_usuario', id)
```

> **Nota:** `updateAsignaciones` y `reenviarCredenciales` no generan eventos de audit separados (ya cubiertos por crear/editar usuario).

- [ ] **Step 2: Agregar imports y llamadas en `app/actions/chequeos.ts`**

Agregar import:
```typescript
import { logAuditEvent } from '@/lib/audit'
```

En `createChequeo`, antes del primer `revalidatePath`:
```typescript
await logAuditEvent('crear_chequeo', `${sucursal_id}/${año}`)
```

En `updateChequeo`, antes del primer `revalidatePath`:
```typescript
await logAuditEvent('editar_chequeo', `${sucursal_id}/${año}`)
```

En `deleteChequeo`, antes del `revalidatePath`:
```typescript
await logAuditEvent('eliminar_chequeo', id)
```

- [ ] **Step 3: Agregar imports y llamadas en `app/actions/chequeos-pre-empleo.ts`**

Agregar import:
```typescript
import { logAuditEvent } from '@/lib/audit'
```

En `createChequeoPreEmpleo`, antes del primer `revalidatePath`:
```typescript
await logAuditEvent('crear_chequeo_pre_empleo', `${sucursal_id}/${año}/${mes}/${dia}`)
```

En `updateChequeoPreEmpleo`, antes del primer `revalidatePath`:
```typescript
await logAuditEvent('editar_chequeo_pre_empleo', `${sucursal_id}/${año}/${mes}/${dia}`)
```

En `deleteChequeoPreEmpleo`, antes del `revalidatePath`:
```typescript
await logAuditEvent('eliminar_chequeo_pre_empleo', id)
```

- [ ] **Step 4: Agregar imports y llamadas en `app/actions/settings.ts`**

Agregar import:
```typescript
import { logAuditEvent } from '@/lib/audit'
```

En `updateProfile`, después del `if (error)` block (solo cuando no hay error):
```typescript
await logAuditEvent('cambio_nombre', nombre)
```

En `updateEmail`, después del `if (error)` block:
```typescript
await logAuditEvent('cambio_correo', email)
```

En `updatePassword`, al final (después del último `if (error)` block):
```typescript
await logAuditEvent('cambio_contraseña')
```

- [ ] **Step 5: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Expected: sin errores.

- [ ] **Step 6: Commit**

```bash
git add app/actions/usuarios.ts app/actions/chequeos.ts app/actions/chequeos-pre-empleo.ts app/actions/settings.ts
git commit -m "feat(audit): log CRUD events for usuarios, chequeos, and settings"
```

---

## Task 6: Drive redirect API route + actualizar hrefs

**Files:**
- Create: `app/api/drive-redirect/route.ts`
- Modify: `app/dashboard/responsable/periodico/[año]/PeriodicoChequeos.tsx`
- Modify: `app/dashboard/responsable/pre-empleo/[año]/[sucursalId]/[mes]/PreEmpleoDaySelector.tsx`

- [ ] **Step 1: Crear `app/api/drive-redirect/route.ts`**

```typescript
import { type NextRequest, NextResponse } from 'next/server'
import { logAuditEvent } from '@/lib/audit'

export async function GET(request: NextRequest) {
  const { searchParams } = new URL(request.url)
  const url = searchParams.get('url') ?? ''

  if (!url.startsWith('https://drive.google.com/')) {
    return new NextResponse('Forbidden', { status: 403 })
  }

  await logAuditEvent('acceso_drive', url)
  return NextResponse.redirect(url)
}
```

- [ ] **Step 2: Actualizar hrefs en `PeriodicoChequeos.tsx`**

Localizar el `<a>` tag con `href={suc.drive_url}` (línea ~114) y reemplazar el `href`:

```tsx
<a
  key={suc.nombre + suc.drive_url}
  href={`/api/drive-redirect?url=${encodeURIComponent(suc.drive_url)}`}
  target="_blank"
  rel="noopener noreferrer"
  className="group bg-surface-container-lowest rounded-2xl border border-outline-variant/20 p-5 flex items-center justify-between hover:shadow-md hover:border-primary/30 transition-all active:scale-[0.98]"
>
```

Solo cambia el `href`. El resto del JSX permanece igual.

- [ ] **Step 3: Actualizar hrefs en `PreEmpleoDaySelector.tsx`**

Localizar el `<a>` tag con `href={entry.drive_url}` (línea ~78) y reemplazar el `href`:

```tsx
<a
  key={entry.dia}
  href={`/api/drive-redirect?url=${encodeURIComponent(entry.drive_url)}`}
  target="_blank"
  rel="noopener noreferrer"
  className="group bg-surface-container-lowest rounded-2xl border border-outline-variant/20 p-4 flex flex-col items-center gap-2 hover:shadow-md hover:border-primary/30 transition-all active:scale-[0.98]"
>
```

Solo cambia el `href`. El resto del JSX permanece igual.

- [ ] **Step 4: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Verificar manualmente**

```bash
pnpm dev
```

1. Login como responsable → navegar a un chequeo periódico → click en una card de sucursal.
2. Verificar que redirige correctamente a Drive.
3. Verificar en Supabase → `audit_log` que existe fila `accion = 'acceso_drive'` con la URL.
4. Intentar acceder a `/api/drive-redirect?url=https://evil.com` — debe retornar 403.

- [ ] **Step 6: Commit**

```bash
git add app/api/drive-redirect/route.ts app/dashboard/responsable/periodico/[año]/PeriodicoChequeos.tsx app/dashboard/responsable/pre-empleo/[año]/[sucursalId]/[mes]/PreEmpleoDaySelector.tsx
git commit -m "feat(audit): add drive-redirect route and log acceso_drive events"
```

---

## Task 7: RSC page — `/dashboard/admin/ajustes/registros/page.tsx`

**Files:**
- Create: `app/dashboard/admin/ajustes/registros/page.tsx`

- [ ] **Step 1: Crear `app/dashboard/admin/ajustes/registros/page.tsx`**

```typescript
import { createClient } from '@/lib/supabase/server'
import { requireAdmin } from '@/lib/auth'
import AuditTable from './AuditTable'
import type { AuditAction } from '@/lib/audit'

const PAGE_SIZE = 50

type SearchParams = Promise<{
  accion?: string
  desde?: string
  hasta?: string
  usuario?: string
  q?: string
  page?: string
}>

export default async function RegistrosPage({ searchParams }: { searchParams: SearchParams }) {
  await requireAdmin()
  const sp = await searchParams

  const accion = sp.accion ?? ''
  const desde = sp.desde ?? ''
  const hasta = sp.hasta ?? ''
  const usuario = sp.usuario ?? ''
  const q = sp.q ?? ''
  const page = Math.max(0, parseInt(sp.page ?? '0', 10))
  const offset = page * PAGE_SIZE

  const supabase = await createClient()

  // Load profiles for filter dropdown
  const { data: profiles } = await supabase
    .from('profiles')
    .select('id, nombre, rol')
    .order('nombre')

  // Build audit log query
  let query = supabase
    .from('audit_log')
    .select('id, accion, recurso, ip_address, user_agent, created_at, usuario_id, profiles(nombre)', {
      count: 'exact',
    })
    .order('created_at', { ascending: false })
    .range(offset, offset + PAGE_SIZE - 1)

  if (accion) query = query.eq('accion', accion)
  if (usuario) query = query.eq('usuario_id', usuario)
  if (desde) query = query.gte('created_at', desde)
  if (hasta) query = query.lte('created_at', hasta + 'T23:59:59')
  if (q) query = query.ilike('recurso', `%${q}%`)

  const { data: rows, count } = await query

  return (
    <AuditTable
      rows={rows ?? []}
      totalCount={count ?? 0}
      profiles={profiles ?? []}
      page={page}
      pageSize={PAGE_SIZE}
      filters={{ accion, desde, hasta, usuario, q }}
    />
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Expected: puede haber advertencias de tipos del join de Supabase — son normales hasta que `AuditTable` esté creado.

---

## Task 8: Client component — `registros/AuditTable.tsx`

**Files:**
- Create: `app/dashboard/admin/ajustes/registros/AuditTable.tsx`

- [ ] **Step 1: Crear `app/dashboard/admin/ajustes/registros/AuditTable.tsx`**

```tsx
'use client'

import { useRouter, useSearchParams } from 'next/navigation'
import { LuChevronLeft, LuChevronRight, LuShieldCheck } from 'react-icons/lu'
import type { AuditAction } from '@/lib/audit'

type AuditRow = {
  id: string
  accion: string
  recurso: string | null
  ip_address: string | null
  user_agent: string | null
  created_at: string
  usuario_id: string | null
  profiles: { nombre: string } | null
}

type Profile = { id: string; nombre: string; rol: string }

type Filters = {
  accion: string
  desde: string
  hasta: string
  usuario: string
  q: string
}

const ALL_ACTIONS: AuditAction[] = [
  'login', 'login_fallido', 'logout',
  'acceso_drive',
  'crear_empresa', 'editar_empresa', 'eliminar_empresa',
  'crear_sucursal', 'editar_sucursal', 'eliminar_sucursal',
  'crear_usuario', 'editar_usuario', 'eliminar_usuario',
  'crear_chequeo', 'editar_chequeo', 'eliminar_chequeo',
  'crear_chequeo_pre_empleo', 'editar_chequeo_pre_empleo', 'eliminar_chequeo_pre_empleo',
  'cambio_nombre', 'cambio_correo', 'cambio_contraseña',
]

function actionBadgeClass(accion: string): string {
  if (accion === 'login_fallido') return 'bg-error-container/40 text-error'
  if (accion === 'login' || accion === 'logout') return 'bg-primary-fixed/40 text-primary'
  if (accion === 'acceso_drive') return 'bg-primary-fixed/40 text-primary'
  if (accion.startsWith('eliminar')) return 'bg-error-container/20 text-on-surface'
  return 'bg-surface-container text-on-surface'
}

function formatDate(iso: string): string {
  return new Date(iso).toLocaleString('es-NI', {
    year: 'numeric',
    month: '2-digit',
    day: '2-digit',
    hour: '2-digit',
    minute: '2-digit',
  })
}

const inputClass =
  'px-3 py-1.5 rounded-lg border border-outline-variant bg-surface-container-low text-sm text-on-surface focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary transition-colors'

export default function AuditTable({
  rows,
  totalCount,
  profiles,
  page,
  pageSize,
  filters,
}: {
  rows: AuditRow[]
  totalCount: number
  profiles: Profile[]
  page: number
  pageSize: number
  filters: Filters
}) {
  const router = useRouter()
  const sp = useSearchParams()

  function setFilter(key: string, value: string) {
    const params = new URLSearchParams(sp.toString())
    if (value) {
      params.set(key, value)
    } else {
      params.delete(key)
    }
    params.delete('page')
    router.push(`?${params.toString()}`)
  }

  function goToPage(p: number) {
    const params = new URLSearchParams(sp.toString())
    params.set('page', String(p))
    router.push(`?${params.toString()}`)
  }

  const totalPages = Math.ceil(totalCount / pageSize)

  return (
    <div className="space-y-6 max-w-6xl">
      {/* Header */}
      <div className="flex items-center gap-3">
        <div className="w-8 h-8 rounded-lg bg-primary-fixed flex items-center justify-center">
          <LuShieldCheck size={16} className="text-primary" />
        </div>
        <div>
          <h2 className="text-sm font-semibold text-on-surface font-headline">Registros de Auditoría</h2>
          <p className="text-xs text-tertiary">{totalCount} eventos totales</p>
        </div>
      </div>

      {/* Filters */}
      <div className="bg-surface-container-lowest rounded-2xl border border-outline-variant/20 p-4">
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5 gap-3">
          {/* Acción */}
          <select
            defaultValue={filters.accion}
            onChange={(e) => setFilter('accion', e.target.value)}
            className={inputClass}
          >
            <option value="">Todas las acciones</option>
            {ALL_ACTIONS.map((a) => (
              <option key={a} value={a}>{a}</option>
            ))}
          </select>

          {/* Usuario */}
          <select
            defaultValue={filters.usuario}
            onChange={(e) => setFilter('usuario', e.target.value)}
            className={inputClass}
          >
            <option value="">Todos los usuarios</option>
            {profiles.map((p) => (
              <option key={p.id} value={p.id}>{p.nombre}</option>
            ))}
          </select>

          {/* Desde */}
          <input
            type="date"
            defaultValue={filters.desde}
            onChange={(e) => setFilter('desde', e.target.value)}
            className={inputClass}
            placeholder="Desde"
          />

          {/* Hasta */}
          <input
            type="date"
            defaultValue={filters.hasta}
            onChange={(e) => setFilter('hasta', e.target.value)}
            className={inputClass}
            placeholder="Hasta"
          />

          {/* Recurso */}
          <input
            type="text"
            defaultValue={filters.q}
            onChange={(e) => setFilter('q', e.target.value)}
            className={inputClass}
            placeholder="Buscar recurso..."
          />
        </div>
      </div>

      {/* Table */}
      <div className="bg-surface-container-lowest rounded-2xl border border-outline-variant/20 overflow-hidden">
        <div className="overflow-x-auto">
          <table className="w-full text-sm">
            <thead>
              <tr className="border-b border-outline-variant/20 bg-surface-container/50">
                <th className="text-left px-4 py-3 text-xs font-semibold text-tertiary uppercase tracking-wide whitespace-nowrap">Fecha/Hora</th>
                <th className="text-left px-4 py-3 text-xs font-semibold text-tertiary uppercase tracking-wide">Usuario</th>
                <th className="text-left px-4 py-3 text-xs font-semibold text-tertiary uppercase tracking-wide">Acción</th>
                <th className="text-left px-4 py-3 text-xs font-semibold text-tertiary uppercase tracking-wide">Recurso</th>
                <th className="text-left px-4 py-3 text-xs font-semibold text-tertiary uppercase tracking-wide">IP</th>
              </tr>
            </thead>
            <tbody className="divide-y divide-outline-variant/10">
              {rows.length === 0 ? (
                <tr>
                  <td colSpan={5} className="px-4 py-12 text-center text-sm text-tertiary">
                    No hay registros para los filtros seleccionados.
                  </td>
                </tr>
              ) : (
                rows.map((row) => (
                  <tr key={row.id} className="hover:bg-surface-container/30 transition-colors">
                    <td className="px-4 py-3 text-xs text-tertiary whitespace-nowrap">
                      {formatDate(row.created_at)}
                    </td>
                    <td className="px-4 py-3 text-xs text-on-surface">
                      {row.profiles?.nombre ?? <span className="text-tertiary italic">desconocido</span>}
                    </td>
                    <td className="px-4 py-3">
                      <span className={`inline-flex items-center px-2 py-0.5 rounded-full text-xs font-medium ${actionBadgeClass(row.accion)}`}>
                        {row.accion}
                      </span>
                    </td>
                    <td className="px-4 py-3 text-xs text-on-surface max-w-xs">
                      {row.recurso ? (
                        <span className="truncate block max-w-[200px]" title={row.recurso}>
                          {row.recurso}
                        </span>
                      ) : (
                        <span className="text-tertiary">—</span>
                      )}
                    </td>
                    <td className="px-4 py-3 text-xs text-tertiary font-mono whitespace-nowrap">
                      {row.ip_address ?? '—'}
                    </td>
                  </tr>
                ))
              )}
            </tbody>
          </table>
        </div>
      </div>

      {/* Pagination */}
      {totalPages > 1 && (
        <div className="flex items-center justify-between">
          <p className="text-xs text-tertiary">
            Página {page + 1} de {totalPages} · {totalCount} registros
          </p>
          <div className="flex items-center gap-2">
            <button
              onClick={() => goToPage(page - 1)}
              disabled={page === 0}
              className="p-1.5 rounded-lg border border-outline-variant text-on-surface disabled:opacity-40 hover:bg-surface-container transition-colors"
            >
              <LuChevronLeft size={16} />
            </button>
            <button
              onClick={() => goToPage(page + 1)}
              disabled={page >= totalPages - 1}
              className="p-1.5 rounded-lg border border-outline-variant text-on-surface disabled:opacity-40 hover:bg-surface-container transition-colors"
            >
              <LuChevronRight size={16} />
            </button>
          </div>
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Verificar TypeScript**

```bash
npx tsc --noEmit
```

Expected: sin errores.

- [ ] **Step 3: Verificar manualmente**

```bash
pnpm dev
```

1. Login como admin → ir a `/dashboard/admin/ajustes/registros`.
2. Confirmar que la tabla carga con los eventos registrados en tasks anteriores.
3. Probar cada filtro: seleccionar una acción → tabla se actualiza. Seleccionar un usuario → filtra. Ingresar rango de fechas → filtra.
4. Probar búsqueda por recurso: escribir un email → muestra solo los eventos con ese email.
5. Si hay >50 registros, verificar que la paginación funciona.

- [ ] **Step 4: Commit**

```bash
git add app/dashboard/admin/ajustes/registros/page.tsx app/dashboard/admin/ajustes/registros/AuditTable.tsx
git commit -m "feat(audit): add audit log viewer at /dashboard/admin/ajustes/registros"
```

---

## Task 9: Agregar link en AjustesClient.tsx

**Files:**
- Modify: `app/dashboard/admin/ajustes/AjustesClient.tsx`

- [ ] **Step 1: Agregar import de Link y LuClipboardList**

Al inicio de `AjustesClient.tsx`, agregar `LuClipboardList` al import de react-icons y `Link` de next/link:

```typescript
import Link from 'next/link'
import { LuUser, LuMail, LuLock, LuShield, LuCheck, LuClipboardList } from 'react-icons/lu'
```

- [ ] **Step 2: Agregar nueva SectionCard al final del return en `AjustesAdminClient`**

Dentro del `<div className="space-y-6 max-w-2xl">`, después del `SectionCard` de Seguridad (MFA), agregar:

```tsx
<SectionCard
  icon={LuClipboardList}
  title="Registros de Auditoría"
  description="Historial de accesos, cambios y eventos de seguridad"
>
  <div className="space-y-3">
    <p className="text-xs text-tertiary">
      Consulta todos los eventos registrados en el sistema: logins, cambios de datos y accesos a Drive.
    </p>
    <Link
      href="/dashboard/admin/ajustes/registros"
      className="inline-flex items-center gap-2 px-4 py-2 rounded-lg text-sm bg-primary text-on-primary font-medium hover:bg-primary-container transition-colors"
    >
      <LuClipboardList size={16} />
      Ver registros
    </Link>
  </div>
</SectionCard>
```

- [ ] **Step 3: Verificar TypeScript**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Verificar manualmente**

```bash
pnpm dev
```

1. Login como admin → ir a `/dashboard/admin/ajustes`.
2. Confirmar que aparece la card "Registros de Auditoría" al final.
3. Click en "Ver registros" → debe navegar a `/dashboard/admin/ajustes/registros` con la tabla de logs.

- [ ] **Step 5: Commit**

```bash
git add app/dashboard/admin/ajustes/AjustesClient.tsx
git commit -m "feat(audit): add audit log link in admin ajustes page"
```

---

## Task 10: Actualizar ISO 27001 plan — marcar Fase 4 completa

**Files:**
- Modify: `docs/ISO-27001-plan.md`

- [ ] **Step 1: Actualizar tabla de controles**

En `docs/ISO-27001-plan.md`, cambiar la fila del control 13:

```markdown
| 13 | Registro y Monitoreo | Registro de accesos y descargas | Técnico | ✅ Completo | ISO 27001 A.12 |
```

- [ ] **Step 2: Actualizar tabla de resumen de fases**

En la sección "Resumen de Entregables", cambiar la fila de Fase 4:

```markdown
| 4 | Técnico | 13 | ✅ Completo |
```

- [ ] **Step 3: Commit final**

```bash
git add docs/ISO-27001-plan.md
git commit -m "docs(iso27001): mark Phase 4 complete - audit logging implemented"
```

---

## Self-Review Checklist

- [x] **DB migration:** tabla `audit_log` con RLS select-only para admin — Task 1
- [x] **Helper `logAuditEvent`:** IP, user-agent, usuario_id nullable — Task 2
- [x] **Auth events:** login, login_fallido (usuarioId null), logout — Task 3
- [x] **CRUD empresas/sucursales** — Task 4
- [x] **CRUD usuarios/chequeos/settings** — Task 5
- [x] **Drive redirect:** validación prefijo `https://drive.google.com/`, 403 si inválido — Task 6
- [x] **Drive hrefs actualizados** en PeriodicoChequeos y PreEmpleoDaySelector — Task 6
- [x] **RSC page** con searchParams + join a profiles — Task 7
- [x] **AuditTable client** con todos los filtros + paginación — Task 8
- [x] **Link desde Ajustes** — Task 9
- [x] **ISO plan actualizado** — Task 10
- [x] **`createAdminClient()`** reutilizado (no se crea nuevo service.ts) — consistente con codebase existente
- [x] **Tipos de `AuditAction`** definidos en `lib/audit.ts` y re-usados en `AuditTable.tsx` via import
